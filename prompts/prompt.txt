feature 1) make a view in landing page a subscription plans in navbar and and build that page where there have three plans plan1 for 1 year, plan2 for 2 years, and plan3 for 4 years

This is the prompt below that needs to be executed, building a complete subscription plans feature:

---

**Task: Subscription Plans Landing Page**

Add a subscription plans link in the landing page navbar and build the subscription plans page with 3 plans (1 year, 2 years, 4 years).

**Files to edit:**

1. **`Routes/HomeRoutes.cs`** — Add `Subscriptions()` route:
   ```csharp
   public static string Subscriptions() => $"{Base}/Subscriptions";
   ```

2. **`Controllers/HomeController.cs`** — Add action:
   ```csharp
   public IActionResult Subscriptions()
   {
       ViewBag.Plans = MockData.GetSubscriptionPlans();
       return View();
   }
   ```

3. **`Data/MockData.cs`** — Add model class `MockSubscriptionPlan` and static method `GetSubscriptionPlans()` returning 3 plans:

   | Plan | Duration | Price | Doctors | Clinics | Support | Reports | Analytics |
   |------|----------|-------|---------|---------|---------|---------|-----------|
   | الباقة الأساسية | 1 year | 2,999 ريال | 5 أطباء | عيادة واحدة | دعم عادي | تقارير أساسية | — |
   | الباقة المتقدمة | 2 years | 4,999 ريال | 15 طبيب | 3 عيادات | دعم ذو أولوية | تقارير متقدمة | تحليلات مدمجة |
   | الباقة الاحترافية | 4 years | 7,999 ريال | غير محدود | غير محدود | دعم VIP 24/7 | تقارير شاملة | ذكاء اصطناعي متكامل |

   Add at bottom of MockData.cs:
   ```csharp
   public class MockSubscriptionPlan
   {
       public string Name { get; set; } = "";
       public string Duration { get; set; } = "";
       public string Price { get; set; } = "";
       public string Currency { get; set; } = "ريال";
       public string Badge { get; set; } = "";
       public string PlanClass { get; set; } = "";
       public string[] Features { get; set; } = Array.Empty<string>();
       public bool[] FeatureEnabled { get; set; } = Array.Empty<bool>();
   }

   public static List<MockSubscriptionPlan> GetSubscriptionPlans() => new()
   {
       new()
       {
           Name = "الباقة الأساسية", Duration = "سنة واحدة", Price = "2,999", Currency = "ريال / سنوياً",
           Badge = "", PlanClass = "plan-free",
           Features = new[] { "حتى 5 أطباء", "عيادة واحدة", "دعم فني عادي", "تقارير أساسية", "تحليلات وإحصائيات" },
           FeatureEnabled = new[] { true, true, true, true, false }
       },
       new()
       {
           Name = "الباقة المتقدمة", Duration = "سنتان", Price = "4,999", Currency = "ريال / سنوياً",
           Badge = "الأكثر طلباً", PlanClass = "plan-standard",
           Features = new[] { "حتى 15 طبيب", "حتى 3 عيادات", "دعم ذو أولوية", "تقارير متقدمة", "تحليلات مدمجة" },
           FeatureEnabled = new[] { true, true, true, true, true }
       },
       new()
       {
           Name = "الباقة الاحترافية", Duration = "4 سنوات", Price = "7,999", Currency = "ريال / سنوياً",
           Badge = "أفضل قيمة", PlanClass = "plan-premium",
           Features = new[] { "أطباء غير محدود", "عيادات غير محدود", "دعم VIP على مدار الساعة", "تقارير شاملة", "ذكاء اصطناعي متكامل" },
           FeatureEnabled = new[] { true, true, true, true, true }
       },
   };
   ```

4. **`Views/Shared/_Layout.cshtml`** — Add navbar link after the "سياسة الخصوصية" link (line ~29):
   ```html
   <a href="@HomeRoutes.Pages.Subscriptions()" class="nav-link">الباقات</a>
   ```

5. **Create `Views/Home/Subscriptions.cshtml`** — The subscription plans page. Use the existing CSS classes from `site.css` (`.subscription-plans`, `.plan-card`, `.plan-free`, `.plan-standard`, `.plan-premium`, `.plan-badge`, `.plan-header`, `.plan-name`, `.plan-price`, `.plan-currency`, `.plan-body`, `.plan-features`, `.plan-footer`, `.plan-current`). Design tokens from `design-system.css` (`--clr-*`, `--space-*`, `--radius-*`, `--fs-*`).

   Layout should be a hero section + plan cards grid + feature comparison table. Use `.btn-primary` for CTA buttons, `.badge` / `.badge-success` / `.badge-warning` / `.badge-info` for status badges, `.icon-wrapper` / `.icon-wrapper--primary` / `--blue` / `--green` / `--amber` for icons.

   The page should be in RTL Arabic, matching the landing page design. Static data display — no complex interactive logic. Each plan card should have a "اشترك الآن" (Subscribe Now) button linking to the Account/Register page.

**Important:** No inline styles. No JavaScript. All styling via CSS classes.
