@{
    ViewData["Title"] = "جدول الطبيب";
    Layout = "_StaffLayout";
    var doctorId = ViewBag.DoctorId as int? ?? 0;
}

<h2 class="page-title">
    <span class="page-title-icon">
        <i class="bi bi-calendar-week"></i>
    </span>
    جدول مواعيد الطبيب
</h2>

<div class="table-card">
    <div class="table-card-header">
        <h3 class="table-card-title" id="scheduleTitle">مواعيد اليوم</h3>
    </div>
    <div class="table-responsive">
        <table class="custom-table">
            <thead>
                <tr>
                    <th>المريض</th>
                    <th>التاريخ</th>
                    <th>الوقت</th>
                    <th>الحالة</th>
                </tr>
            </thead>
            <tbody id="scheduleBody">
                <tr>
                    <td colspan="4" class="table-empty">جاري تحميل البيانات...</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        var doctorId = @doctorId;

        StaffDashboardService.getDoctorSchedule(doctorId).then(function (data) {
            if (data.doctor && data.doctor.name) {
                $('#scheduleTitle').text('مواعيد ' + data.doctor.name);
            }
            StaffDashboardService.renderDoctorSchedule(data);
        });
    </script>
}
