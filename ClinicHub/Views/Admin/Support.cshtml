@using ClinicHub.Services.ReponseModels
@{
    ViewData["Title"] = "الدعم الفني";
    Layout = "_AdminLayout";
    var tickets = ViewBag.Tickets as List<SupportTicketDto> ?? new();
    var statusFilter = ViewBag.StatusFilter as string;
    var priorityFilter = ViewBag.PriorityFilter as string;

    var ticketStatuses = new Dictionary<int, (string Label, string Class)>
    {
        [1] = ("مفتوح", "badge-info"),
        [2] = ("قيد المعالجة", "badge-warning"),
        [3] = ("تم الحل", "badge-success"),
        [4] = ("مغلق", "badge-danger")
    };

    var ticketPriorities = new Dictionary<int, (string Label, string Class)>
    {
        [1] = ("عالية", "badge-danger"),
        [2] = ("متوسطة", "badge-warning"),
        [3] = ("منخفضة", "badge-info")
    };

    (string Label, string Class) StatusOf(int value)
        => ticketStatuses.TryGetValue(value, out var s) ? s : ("غير معروف", "badge-secondary");

    (string Label, string Class) PriorityOf(int value)
        => ticketPriorities.TryGetValue(value, out var p) ? p : ("غير معروف", "badge-secondary");
}

<div class="page-header">
    <h2 class="page-title">
        <span class="page-title-icon">
            <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path d="M19 12v-2c0-3.866-3.134-7-7-7S5 6.134 5 10v2H3v7h3v-9c0-3.313 2.687-6 6-6s6 2.687 6 6v9h3v-7h-2zM7 15v2c0 1.103.897 2 2 2h2v-6H7v2zm8 0h-2v2h2c1.103 0 2-.897 2-2v-2h-2v2z" fill="currentColor"/>
            </svg>
        </span>
        تذاكر الدعم الفني
    </h2>
</div>

<!-- Filter Bar -->
<form class="filter-bar" method="get" action="/Admin/Support">
    <select id="filterStatus" name="status" class="form-select" onchange="this.form.submit()">
        <option value="">جميع الحالات</option>
        <option value="1" selected="@(statusFilter == "1")">مفتوح</option>
        <option value="2" selected="@(statusFilter == "2")">قيد المعالجة</option>
        <option value="3" selected="@(statusFilter == "3")">تم الحل</option>
        <option value="4" selected="@(statusFilter == "4")">مغلق</option>
    </select>
    <select id="filterPriority" name="priority" class="form-select" onchange="this.form.submit()">
        <option value="">جميع الأولويات</option>
        <option value="1" selected="@(priorityFilter == "1")">عالية</option>
        <option value="2" selected="@(priorityFilter == "2")">متوسطة</option>
        <option value="3" selected="@(priorityFilter == "3")">منخفضة</option>
    </select>
</form>

<!-- Tickets Table -->
<div class="table-card">
    <div class="table-responsive">
        <table class="custom-table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>الموضوع</th>
                    <th>المبلغ</th>
                    <th>الأولوية</th>
                    <th>الحالة</th>
                    <th>تاريخ الإبلاغ</th>
                    <th>المرفقات</th>
                    <th>الإجراءات</th>
                </tr>
            </thead>
            <tbody>
                @{
                    var ticketIndex = 0;
                }
                @foreach (var t in tickets)
                {
                    var tStatus = StatusOf(t.Status);
                    var tPriority = PriorityOf(t.Priority);
                    ticketIndex++;
                    <tr>
                        <td class="text-md">#@t.Id.ToString().Split('-')[0].ToUpper()</td>
                        <td class="text-md-medium">
                            <span class="row-icon @(tPriority.Label == "عالية" ? "text-danger" : "text-muted")">
                                <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z" fill="currentColor"/>
                                </svg>
                            </span>
                            @t.Subject
                        </td>
                        <td class="text-md">@t.UserName</td>
                        <td><span class="badge @tPriority.Class">@tPriority.Label</span></td>
                        <td><span class="badge @tStatus.Class">@tStatus.Label</span></td>
                        <td class="text-md">@t.CreatedAt.ToString("d MMMM yyyy")</td>
                        <td class="text-md">
                            <span class="text-md">—</span>
                        </td>
                        <td>
                            <div class="table-actions">
                                <button class="btn-icon btn-view-ticket" title="عرض التفاصيل"
                                        data-code="#@t.Id.ToString().Split('-')[0].ToUpper()"
                                        data-subject="@t.Subject"
                                        data-reporter="@t.UserName"
                                        data-priority="@tPriority.Label"
                                        data-priority-class="@tPriority.Class"
                                        data-status="@tStatus.Label"
                                        data-status-class="@tStatus.Class"
                                        data-date="@t.CreatedAt.ToString("d MMMM yyyy")"
                                        data-description="@t.Description"
                                        data-has-attachment="false"
                                        data-attachments="">
                                    <svg viewBox="0 0 24 24" fill="currentColor">
                                        <path d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>
                                    </svg>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
                @if (tickets.Count == 0)
                {
                    <tr>
                        <td colspan="8" class="text-md text-center">لا توجد تذاكر دعم مطابقة</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<partial name="_Pagination" />

<!-- Ticket Detail Modal -->
<div class="modal fade" id="ticketDetailModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content modal-content-custom">
            <div class="modal-header-custom">
                <h5 class="modal-title" id="ticketModalTitle">تفاصيل التذكرة</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="إغلاق"></button>
            </div>
            <div class="modal-body-custom" id="ticketModalBody">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <p class="form-label">المبلغ:</p>
                        <p class="text-md-medium" id="ticketReporter"></p>
                    </div>
                    <div class="col-md-3">
                        <p class="form-label">الأولوية:</p>
                        <span id="ticketPriority"></span>
                    </div>
                    <div class="col-md-3">
                        <p class="form-label">الحالة:</p>
                        <span id="ticketStatus"></span>
                    </div>
                </div>
                <div class="form-group">
                    <p class="form-label">الموضوع:</p>
                    <p class="text-md-medium" id="ticketSubject"></p>
                </div>
                <div class="form-group">
                    <p class="form-label">الوصف:</p>
                    <p class="text-md" id="ticketDescription"></p>
                </div>
                <div class="form-group" id="ticketAttachmentsGroup" style="display:none;">
                    <p class="form-label">المرفقات:</p>
                    <div id="ticketAttachments" class="d-flex gap-2 align-items-center"></div>
                </div>
                <div class="form-group">
                    <p class="form-label">تاريخ الإبلاغ:</p>
                    <p class="text-md" id="ticketDate"></p>
                </div>
                <hr />
                <div class="form-group">
                    <label class="form-label">إضافة رد</label>
                    <textarea class="form-textarea" placeholder="اكتب ردك هنا..." rows="3"></textarea>
                </div>
                <div class="form-group">
                    <label class="form-label">تغيير الحالة إلى</label>
                    <select class="form-select">
                        <option>قيد المعالجة</option>
                        <option>تم الحل</option>
                        <option>مغلق</option>
                    </select>
                </div>
            </div>
            <div class="modal-footer-custom">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">إغلاق</button>
                <button type="button" class="btn btn-primary">إرسال الرد</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        (function() {
            var ticketModal = new bootstrap.Modal(document.getElementById('ticketDetailModal'));

            document.querySelectorAll('.btn-view-ticket').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    document.getElementById('ticketModalTitle').textContent = 'تفاصيل التذكرة ' + btn.getAttribute('data-code');
                    document.getElementById('ticketReporter').textContent = btn.getAttribute('data-reporter');
                    document.getElementById('ticketSubject').textContent = btn.getAttribute('data-subject');
                    document.getElementById('ticketDescription').textContent = btn.getAttribute('data-description');
                    document.getElementById('ticketDate').textContent = btn.getAttribute('data-date');

                    var priorityEl = document.getElementById('ticketPriority');
                    priorityEl.innerHTML = '<span class="badge ' + btn.getAttribute('data-priority-class') + '">' + btn.getAttribute('data-priority') + '</span>';

                    var statusEl = document.getElementById('ticketStatus');
                    statusEl.innerHTML = '<span class="badge ' + btn.getAttribute('data-status-class') + '">' + btn.getAttribute('data-status') + '</span>';

                    var hasAttachment = btn.getAttribute('data-has-attachment') === 'true';
                    var attachmentsGroup = document.getElementById('ticketAttachmentsGroup');
                    var attachmentsContainer = document.getElementById('ticketAttachments');
                    if (hasAttachment) {
                        attachmentsGroup.style.display = '';
                        attachmentsContainer.innerHTML = '';
                        var atts = btn.getAttribute('data-attachments').split(',');
                        atts.forEach(function(a) {
                            var span = document.createElement('span');
                            span.className = 'badge badge-info';
                            span.textContent = a.trim();
                            attachmentsContainer.appendChild(span);
                        });
                    } else {
                        attachmentsGroup.style.display = 'none';
                    }

                    ticketModal.show();
                });
            });
        })();
    </script>
}
